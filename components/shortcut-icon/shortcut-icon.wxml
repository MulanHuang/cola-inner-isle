<!-- 🎨 快捷入口图标组件 - 使用图片图标 -->
<view class="shortcut-icon-container">
  <!-- 情绪记录图标（心形） -->
  <image
    wx:if="{{name === 'emotion'}}"
    class="shortcut-icon-image"
    src="/images/shortcuts/emotion.svg"
    mode="aspectFit"
  />

  <!-- 心语/对话图标（消息气泡） -->
  <image
    wx:elif="{{name === 'chat'}}"
    class="shortcut-icon-image"
    src="/images/shortcuts/chat.svg"
    mode="aspectFit"
  />

  <!-- 冥想图标（莲花） -->
  <image
    wx:elif="{{name === 'meditation'}}"
    class="shortcut-icon-image"
    src="/images/shortcuts/meditation.svg"
    mode="aspectFit"
  />
</view>
