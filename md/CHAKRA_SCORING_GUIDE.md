# 🎯 脉轮测试评分规则详解

## 📋 一、题目分配表

### 完整题目映射

| 脉轮 | 英文Key | 中文名称 | 题目ID | 题目数量 |
|------|---------|----------|--------|----------|
| 🔴 | root | 海底轮 | 1-12 | 12题 |
| 🟠 | sacral | 腹轮 | 13-23 | 11题 |
| 🟡 | solar | 太阳神经丛轮 | 24-35 | 12题 |
| 💚 | heart | 心轮 | 36-47 | 12题 |
| 🔵 | throat | 喉轮 | 48-58 | 11题 |
| 🟣 | third_eye | 眉心轮 | 59-69 | 11题 |
| ⚪ | crown | 顶轮 | 70-80 | 11题 |
| **总计** | - | - | **1-80** | **80题** |

---

## 🔄 二、评分逻辑

### 1. 题目类型

**所有80道题目都是负向题（reverse: true）**

- **负向题**：分数越高 = 问题越严重 = 脉轮能量越弱
- 例如："你是否经常感到焦虑？"
  - 选5分（完全是）→ 表示经常焦虑 → 脉轮能量弱
  - 选1分（完全不是）→ 表示不焦虑 → 脉轮能量强

### 2. 反转计分公式

```javascript
// 原始分数（用户选择）：1-5分
// 反转后分数：reversedScore = 6 - originalScore

示例：
用户选1分 → 反转后 = 6 - 1 = 5分（能量强）
用户选2分 → 反转后 = 6 - 2 = 4分
用户选3分 → 反转后 = 6 - 3 = 3分（中等）
用户选4分 → 反转后 = 6 - 4 = 2分
用户选5分 → 反转后 = 6 - 5 = 1分（能量弱）
```

### 3. 脉轮得分计算

```javascript
// 步骤1：筛选该脉轮的所有题目
const chakraQuestions = questions.filter(q => q.chakra === 'root');

// 步骤2：计算反转后的总分
let totalScore = 0;
chakraQuestions.forEach(q => {
  const userAnswer = answers[q.id]; // 用户选择的分数（1-5）
  const reversedScore = 6 - userAnswer; // 反转分数
  totalScore += reversedScore;
});

// 步骤3：计算最大可能分数
const maxScore = chakraQuestions.length * 5; // 每题最高5分

// 步骤4：计算百分比
const percentage = (totalScore / maxScore) * 100;
```

### 4. 实际计算示例

**海底轮（12题）**

假设用户答案：
- 题1-6：都选了5分（完全是）
- 题7-12：都选了1分（完全不是）

计算过程：
```
题1-6反转分数：6-5=1，共6题 → 1×6 = 6分
题7-12反转分数：6-1=5，共6题 → 5×6 = 30分
总分 = 6 + 30 = 36分
最大分 = 12题 × 5分 = 60分
百分比 = (36 / 60) × 100 = 60%
```

结果：海底轮能量 60%（基本平衡）

---

## 📊 三、评分区间定义

### 所有脉轮统一标准

| 百分比范围 | 等级 | 标题 | 说明 |
|-----------|------|------|------|
| **0-39%** | 低分 | 能量偏弱/阻塞 | 该脉轮能量严重不足，需要重点疗愈 |
| **40-69%** | 中分 | 基本平衡/轻微失衡 | 该脉轮基本平衡，但仍有提升空间 |
| **70-100%** | 高分 | 流动良好/充盈 | 该脉轮能量充沛，状态良好 |

### 各脉轮具体解读

#### 🔴 海底轮 (Root)
- **低分 (0-39%)**：能量偏弱 · 需要滋养
- **中分 (40-69%)**：基本平衡 · 持续滋养
- **高分 (70-100%)**：能量充沛 · 根基稳固

#### 🟠 腹轮 (Sacral)
- **低分 (0-39%)**：能量偏弱 · 需要流动
- **中分 (40-69%)**：基本平衡 · 持续流动
- **高分 (70-100%)**：能量充沛 · 创造力丰沛

#### 🟡 太阳神经丛轮 (Solar Plexus)
- **低分 (0-39%)**：能量偏弱 · 需要力量
- **中分 (40-69%)**：基本平衡 · 持续成长
- **高分 (70-100%)**：能量充沛 · 力量强大

#### 💚 心轮 (Heart)
- **低分 (0-39%)**：能量偏弱 · 需要疗愈
- **中分 (40-69%)**：基本平衡 · 持续开放
- **高分 (70-100%)**：能量充沛 · 爱意流动

#### 🔵 喉轮 (Throat)
- **低分 (0-39%)**：能量偏弱 · 需要表达
- **中分 (40-69%)**：基本平衡 · 持续表达
- **高分 (70-100%)**：能量充沛 · 真实表达

#### 🟣 眉心轮 (Third Eye)
- **低分 (0-39%)**：能量偏弱 · 需要觉醒
- **中分 (40-69%)**：基本平衡 · 持续觉察
- **高分 (70-100%)**：能量充沛 · 洞察清晰

#### ⚪ 顶轮 (Crown)
- **低分 (0-39%)**：能量偏弱 · 需要连接
- **中分 (40-69%)**：基本平衡 · 持续探索
- **高分 (70-100%)**：能量充沛 · 灵性觉醒

---

## 💻 四、代码实现

### 完整评分函数

```javascript
/**
 * 计算单个脉轮的得分
 * @param {Object} answers - 用户答案对象 {questionId: score}
 * @param {String} chakraType - 脉轮类型 (root/sacral/solar/heart/throat/third_eye/crown)
 * @returns {Object} - {score: 实际得分, maxScore: 最大分数, percentage: 百分比}
 */
function calculateChakraScore(answers, chakraType) {
  // 1. 获取该脉轮的所有题目
  const chakraQuestions = CHAKRA_QUESTIONS.filter(q => q.chakra === chakraType);
  
  // 2. 计算反转后的总分
  let totalScore = 0;
  chakraQuestions.forEach(question => {
    const userAnswer = answers[question.id] || 3; // 默认3分（中等）
    const reversedScore = 6 - userAnswer; // 反转计分
    totalScore += reversedScore;
  });
  
  // 3. 计算最大分数和百分比
  const maxScore = chakraQuestions.length * 5;
  const percentage = Math.round((totalScore / maxScore) * 100);
  
  return {
    score: totalScore,
    maxScore: maxScore,
    percentage: percentage
  };
}

/**
 * 获取脉轮解读
 * @param {Number} percentage - 百分比分数
 * @param {String} chakraType - 脉轮类型
 * @returns {Object} - 解读对象
 */
function getChakraInterpretation(percentage, chakraType) {
  const chakraConfig = CHAKRA_RESULT_CONFIG.find(c => c.key === chakraType);
  
  if (percentage >= 70) {
    return chakraConfig.scoreRanges.high;
  } else if (percentage >= 40) {
    return chakraConfig.scoreRanges.medium;
  } else {
    return chakraConfig.scoreRanges.low;
  }
}
```

---

## 📈 五、结果示例

### 完整测试结果

```json
{
  "root": {
    "score": 45,
    "maxScore": 60,
    "percentage": 75,
    "level": "high",
    "title": "能量充沛 · 根基稳固"
  },
  "sacral": {
    "score": 30,
    "maxScore": 55,
    "percentage": 55,
    "level": "medium",
    "title": "基本平衡 · 持续流动"
  },
  "solar": {
    "score": 20,
    "maxScore": 60,
    "percentage": 33,
    "level": "low",
    "title": "能量偏弱 · 需要力量"
  }
  // ... 其他4个脉轮
}
```

---

## ✅ 六、使用方法

### 在小程序中使用

1. **导入配置文件**
```javascript
const { CHAKRA_MAPPING } = require('./data/chakraMapping.js');
const { CHAKRA_RESULT_CONFIG } = require('./data/chakraResultConfig.js');
```

2. **计算所有脉轮得分**
```javascript
const results = {};
const chakraTypes = ['root', 'sacral', 'solar', 'heart', 'throat', 'third_eye', 'crown'];

chakraTypes.forEach(type => {
  results[type] = calculateChakraScore(answers, type);
});
```

3. **获取解读和建议**
```javascript
chakraTypes.forEach(type => {
  const interpretation = getChakraInterpretation(results[type].percentage, type);
  console.log(`${type}: ${interpretation.title}`);
  console.log(`描述: ${interpretation.description}`);
  console.log(`建议: ${interpretation.lifeAdvice.join(', ')}`);
});
```

---

## 🎉 完成！

现在你拥有了完整的脉轮测试评分系统，包括：

✅ 80道题目的脉轮映射
✅ 清晰的反转计分逻辑
✅ 三档评分区间定义
✅ 7个脉轮的详细解读
✅ 可直接使用的代码实现

所有配置都可以直接复制到微信小程序中使用！

