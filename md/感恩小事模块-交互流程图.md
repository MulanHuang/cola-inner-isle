# 🙏 感恩小事模块 - 交互流程图

## 📊 智能插入逻辑流程

```
用户点击模板按钮
        ↓
┌───────────────────────────────────┐
│  检查：是否有输入框正在聚焦？      │
└───────────────────────────────────┘
        ↓
    ┌───┴───┐
    │  是   │  否
    ↓       ↓
┌─────────┐  ┌─────────────────────────┐
│ 插入到  │  │ 检查：是否有空白输入框？ │
│ 聚焦的  │  └─────────────────────────┘
│ 输入框  │          ↓
└─────────┘      ┌───┴───┐
    ↓            │  是   │  否
    │            ↓       ↓
    │      ┌─────────┐  ┌─────────┐
    │      │ 插入到  │  │ 插入到  │
    │      │ 第一个  │  │ 第一个  │
    │      │ 空白的  │  │ 输入框  │
    │      │ 输入框  │  │ (兜底)  │
    │      └─────────┘  └─────────┘
    │            ↓           ↓
    └────────────┴───────────┘
                 ↓
    ┌───────────────────────────────┐
    │ 检查：是否已有相同模板前缀？   │
    └───────────────────────────────┘
                 ↓
            ┌────┴────┐
            │  是     │  否
            ↓         ↓
    ┌──────────────┐  ┌──────────────┐
    │ 不重复插入   │  │ 插入模板前缀 │
    │ 只聚焦并定位 │  │ 并设置光标   │
    └──────────────┘  └──────────────┘
            ↓                 ↓
            └─────────┬───────┘
                      ↓
            ┌──────────────────┐
            │ 更新聚焦状态     │
            │ 更新光标位置     │
            │ 触发视觉反馈     │
            └──────────────────┘
                      ↓
                  完成插入
```

---

## 🎯 用户操作流程

### 流程1：快速记录模式

```
用户操作                    系统响应
────────────────────────────────────────────
点击模板按钮1               → 检测无聚焦输入框
"🙏 今天我感激的是…"        → 找到第1个空白输入框
                            → 插入模板前缀
                            → 光标定位到前缀后方
                            → 输入框聚焦（边框变粉色）
                            → 编号圆圈放大
                            ↓
输入内容："家人的关心"      → 记录输入内容
                            → 记录光标位置
                            ↓
点击模板按钮2               → 检测无聚焦输入框
"💝 让我微笑的瞬间…"        → 找到第2个空白输入框
                            → 插入模板前缀
                            → 光标定位到前缀后方
                            ↓
输入内容："下班路上的夕阳"  → 记录输入内容
                            ↓
点击模板按钮3               → 检测无聚焦输入框
"🌸 温暖我的人或事…"        → 找到第3个空白输入框
                            → 插入模板前缀
                            → 光标定位到前缀后方
                            ↓
输入内容："同事请我喝咖啡"  → 记录输入内容
                            ↓
                        完成3个感恩事项记录
```

### 流程2：精准填写模式

```
用户操作                    系统响应
────────────────────────────────────────────
点击第2个输入框             → 输入框获得焦点
                            → 边框变为粉色
                            → 编号圆圈放大
                            → 记录聚焦索引：2
                            → 模板按钮显示激活状态
                            ↓
点击模板按钮                → 检测到聚焦输入框：第2个
"💝 让我微笑的瞬间…"        → 插入到第2个输入框
                            → 光标定位到前缀后方
                            ↓
输入内容："下班路上的夕阳"  → 记录输入内容
                            → 记录光标位置
                            ↓
点击第1个输入框             → 第2个输入框失去焦点
                            → 第1个输入框获得焦点
                            → 记录聚焦索引：1
                            ↓
点击模板按钮                → 检测到聚焦输入框：第1个
"🙏 今天我感激的是…"        → 插入到第1个输入框
                            → 光标定位到前缀后方
                            ↓
输入内容："家人的关心"      → 记录输入内容
                            ↓
                        完成2个感恩事项记录
```

---

## 🎨 视觉反馈流程

### 输入框聚焦流程

```
用户点击输入框
    ↓
触发 onFocus 事件
    ↓
更新数据：
- currentFocusedGratitudeIndex = 当前索引
- gratitudeFocusStates[索引] = true
    ↓
触发视觉变化：
┌─────────────────────────────────┐
│ 输入框边框：淡粉 → 深粉 #ff9a8b │
│ 输入框阴影：增强                │
│ 输入框背景：半透明 → 不透明     │
│ 编号圆圈：放大 1.1 倍           │
│ 编号圆圈阴影：增强              │
│ 模板按钮：显示激活状态          │
└─────────────────────────────────┘
```

### 输入框失焦流程

```
用户点击其他区域
    ↓
触发 onBlur 事件
    ↓
更新数据：
- gratitudeFocusStates[索引] = false
    ↓
延迟 200ms 后：
- currentFocusedGratitudeIndex = -1
    ↓
触发视觉变化：
┌─────────────────────────────────┐
│ 输入框边框：深粉 → 淡粉         │
│ 输入框阴影：减弱                │
│ 输入框背景：不透明 → 半透明     │
│ 编号圆圈：恢复原大小            │
│ 编号圆圈阴影：减弱              │
│ 模板按钮：取消激活状态          │
└─────────────────────────────────┘
```

### 模板按钮点击流程

```
用户点击模板按钮
    ↓
触发 hover-class 效果
┌─────────────────────────────────┐
│ 按钮缩放：scale(0.95)           │
│ 按钮背景：加深                  │
│ 持续时间：100ms                 │
└─────────────────────────────────┘
    ↓
触发 insertGratitudeTemplate 方法
    ↓
执行智能插入逻辑
    ↓
更新数据：
- gratitudeItems[目标索引] = 模板前缀
- gratitudeFocusStates[目标索引] = true
- gratitudeCursorPositions[目标索引] = 前缀长度
    ↓
触发视觉变化：
┌─────────────────────────────────┐
│ 目标输入框：显示聚焦状态        │
│ 输入框内容：显示模板前缀        │
│ 光标位置：定位到前缀后方        │
└─────────────────────────────────┘
```

---

## 🔄 状态管理流程

### 数据流向

```
用户交互
    ↓
事件触发 (onFocus / onBlur / insertTemplate / onInput)
    ↓
更新 Page.data
┌─────────────────────────────────────────┐
│ - gratitudeItems: ["", "", ""]          │
│ - currentFocusedGratitudeIndex: -1      │
│ - gratitudeFocusStates: [false, ...]    │
│ - gratitudeCursorPositions: [0, 0, 0]   │
└─────────────────────────────────────────┘
    ↓
触发 setData()
    ↓
WXML 重新渲染
┌─────────────────────────────────────────┐
│ - input value 更新                      │
│ - input focus 状态更新                  │
│ - input cursor 位置更新                 │
│ - class 样式更新 (focused / active)     │
└─────────────────────────────────────────┘
    ↓
WXSS 样式生效
┌─────────────────────────────────────────┐
│ - 边框颜色变化                          │
│ - 阴影效果变化                          │
│ - 编号圆圈缩放                          │
│ - 模板按钮激活状态                      │
└─────────────────────────────────────────┘
    ↓
用户看到视觉反馈
```

---

## 💡 关键技术点

### 1. 延迟清除聚焦索引

```javascript
// 为什么需要延迟 200ms？
setTimeout(() => {
  if (this.data.currentFocusedGratitudeIndex === index) {
    this.setData({ currentFocusedGratitudeIndex: -1 });
  }
}, 200);

// 原因：
// 用户点击模板按钮时，输入框会先失焦（触发 onBlur）
// 如果立即清除聚焦索引，模板按钮点击时就无法获取聚焦状态
// 延迟 200ms 可以确保模板按钮的点击事件先执行
```

### 2. 防止重复插入

```javascript
// 检查是否已有相同模板前缀
const currentValue = gratitudeItems[targetIndex];
if (currentValue && currentValue.startsWith(templateText)) {
  // 已有相同模板前缀，不重复插入
  // 只聚焦并定位光标
  return;
}

// 好处：
// - 避免用户困惑（为什么点击没反应？）
// - 提供明确的视觉反馈（聚焦和光标定位）
// - 保护用户已输入的内容
```

### 3. 光标自动定位

```javascript
// 设置光标位置为模板前缀的长度
gratitudeCursorPositions[targetIndex] = templateText.length;

// 配合 WXML 的 cursor 属性
<input cursor="{{gratitudeCursorPositions[index]}}" />

// 效果：
// 模板插入后，光标自动定位到前缀后方
// 用户可以立即继续输入，无需手动调整光标
```

---

**版本：** v3.1 - 感恩小事交互优化  
**创建时间：** 2025-11-17

