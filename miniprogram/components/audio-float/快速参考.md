# 🎵 全局悬浮音乐控制按钮 - 快速参考

## 📦 组件文件

```
components/audio-float/
├── audio-float.js      # 组件逻辑
├── audio-float.wxml    # 组件模板
├── audio-float.wxss    # 组件样式
└── audio-float.json    # 组件配置
```

---

## 🚀 使用方法

### 方法1：全局注册（已完成）

**app.json：**
```json
{
  "usingComponents": {
    "audio-float": "/components/audio-float/audio-float"
  }
}
```

**任意页面 xxx.wxml：**
```xml
<audio-float />
```

### 方法2：页面级注册

**pages/xxx/xxx.json：**
```json
{
  "usingComponents": {
    "audio-float": "/components/audio-float/audio-float"
  }
}
```

**pages/xxx/xxx.wxml：**
```xml
<audio-float />
```

---

## ✅ 已集成页面

- ✅ pages/home/home.wxml
- ✅ pages/chat/chat.wxml
- ✅ pages/meditation/meditation.wxml
- ✅ pages/emotion/emotion.wxml
- ✅ pages/profile/profile.wxml

---

## 🎨 样式调整

**修改位置：** `components/audio-float/audio-float.wxss`

```css
.audio-float-container {
  right: 30rpx;   /* 右边距 */
  bottom: 120rpx; /* 底部距离 */
}
```

---

## 🔧 功能说明

| 功能 | 说明 |
|------|------|
| 自动显示 | 音频播放时自动显示 |
| 自动隐藏 | 音频停止时自动隐藏 |
| 显示标题 | 显示当前音频标题 |
| 播放指示 | 绿色小点 + 脉冲动画 |
| 一键停止 | 点击按钮停止播放 |

---

## 📱 测试步骤

1. 编译小程序
2. 进入冥想页面
3. 播放任意音频
4. 观察右下角悬浮按钮
5. 切换到其他页面
6. 点击按钮停止播放

---

## ⚠️ 注意事项

1. 播放器页面不需要添加此组件
2. 默认位置避免与 tabBar 重叠
3. 组件销毁时自动清理事件监听
4. 全局单例，所有页面共享

---

## 💡 快速修改

### 修改按钮颜色
```css
.audio-float-button {
  background: rgba(139, 115, 85, 0.9); /* 改为主题色 */
}
```

### 修改小点颜色
```css
.status-dot.playing {
  background: #FF6B6B; /* 改为红色 */
}
```

### 修改文字大小
```css
.audio-title {
  font-size: 28rpx; /* 调整字号 */
}
```

---

## 🐛 常见问题

**Q：按钮不显示？**
A：确保有音频在播放，检查组件是否正确注册

**Q：点击没反应？**
A：检查控制台错误信息

**Q：位置不合适？**
A：修改 `.audio-float-container` 的 `right` 和 `bottom` 值

---

**完成时间：** 2025-11-18  
**开发者：** Augment Agent

