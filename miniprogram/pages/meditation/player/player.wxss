/* pages/meditation/player/player.wxss */
/* 🎨 高端冥想播放器 - 治愈系设计 */

.player-container {
  position: relative;
  height: 100vh;
  padding: 0;
  overflow: hidden;
  background: linear-gradient(180deg, #fff5f0 0%, #ffe8e0 50%, #ffd4c8 100%);
}

/* 背景 - 柔和渐变层  */
.player-bg {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-size: cover;
  background-position: center;
  filter: blur(60rpx) brightness(1.1);
  transform: scale(1.3);
  opacity: 0.4;
}

.player-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    180deg,
    rgba(255, 245, 240, 0.92) 0%,
    rgba(255, 232, 224, 0.88) 50%,
    rgba(255, 212, 200, 0.85) 100%
  );
  backdrop-filter: blur(20rpx);
}

/* 内容 */
.player-content {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  height: 100vh;
  padding: 60rpx 48rpx 80rpx;
  box-sizing: border-box;
}

/* 封面 - 更精致的阴影和光晕 */
.cover-wrapper {
  width: 480rpx;
  height: 480rpx;
  margin-top: 20rpx;
  margin-bottom: 0;
  flex-shrink: 0;
  position: relative;
}

.cover-wrapper::before {
  content: "";
  position: absolute;
  top: -20rpx;
  left: -20rpx;
  right: -20rpx;
  bottom: -20rpx;
  background: radial-gradient(
    circle,
    rgba(255, 182, 193, 0.3) 0%,
    rgba(255, 218, 185, 0.2) 50%,
    transparent 70%
  );
  border-radius: 50%;
  animation: pulse 3s ease-in-out infinite;
}

@keyframes pulse {
  0%,
  100% {
    opacity: 0.6;
    transform: scale(1);
  }
  50% {
    opacity: 1;
    transform: scale(1.05);
  }
}

.cover-image {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  box-shadow: 0 24rpx 80rpx rgba(255, 140, 140, 0.25),
    0 8rpx 24rpx rgba(255, 182, 193, 0.2), 0 0 0 1rpx rgba(255, 255, 255, 0.8);
  position: relative;
  z-index: 1;
}

.cover-image.rotating {
  animation: rotate 30s linear infinite;
}

@keyframes rotate {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

/* 音频信息 - 优雅字体 */
.audio-info {
  text-align: center;
  margin: 40rpx 0 24rpx;
  padding: 0 40rpx;
  flex-shrink: 0;
}

.audio-title {
  font-size: 44rpx;
  font-weight: 500;
  color: #4a3428;
  margin-bottom: 20rpx;
  letter-spacing: 1rpx;
  line-height: 1.4;
}

.audio-desc {
  font-size: 26rpx;
  font-weight: 300;
  color: #8b7355;
  line-height: 1.6;
  letter-spacing: 0.5rpx;
  opacity: 0.85;
}

/* ========== 旗舰级冥想播放器 - 下半部分 ========== */

.bottom-section {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: auto;
  padding-bottom: 24rpx;
  position: relative;
}

/* 底部柔光背景层 - 增加层次感 */
.bottom-glow {
  position: absolute;
  top: -100rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 600rpx;
  height: 400rpx;
  background: radial-gradient(
    ellipse at center,
    rgba(255, 220, 210, 0.25) 0%,
    rgba(255, 200, 190, 0.1) 40%,
    transparent 70%
  );
  pointer-events: none;
  z-index: 0;
}

/* -------- 进度条区域 - Apple Music 风格 -------- */
.progress-section {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 48rpx;
  margin-bottom: 64rpx;
  position: relative;
  z-index: 1;
}

.progress-time {
  font-size: 24rpx;
  font-weight: 400;
  font-variant-numeric: tabular-nums;
  color: rgba(168, 116, 94, 0.8);
  min-width: 76rpx;
  letter-spacing: 0.5rpx;
}

.time-left {
  text-align: right;
  margin-right: 24rpx;
}

.time-right {
  text-align: left;
  margin-left: 24rpx;
}

.progress-track {
  flex: 1;
  max-width: 72%;
  position: relative;
}

.progress-slider {
  width: 100%;
  margin: 0;
}

/* -------- 主播放控制区 - 三个圆形按钮 -------- */
.play-controls {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 60rpx;
  width: 100%;
  margin-bottom: 56rpx;
  position: relative;
  z-index: 1;
}

.ctrl-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  flex-shrink: 0;
  position: relative;
}

.ctrl-btn:active {
  transform: scale(0.9);
}

/* 左右两侧按钮 - 白色圆形 */
.ctrl-btn-side {
  width: 104rpx;
  height: 104rpx;
  background: #ffffff;
  box-shadow: 0 6rpx 24rpx rgba(168, 116, 94, 0.12),
    0 2rpx 8rpx rgba(0, 0, 0, 0.04), inset 0 1rpx 0 rgba(255, 255, 255, 1);
}

.ctrl-btn-side:active {
  box-shadow: 0 2rpx 12rpx rgba(168, 116, 94, 0.15),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.8);
}

.ctrl-btn-side .ctrl-icon {
  width: 38rpx;
  height: 38rpx;
}

/* 中间主按钮 - 粉色渐变 + 发光效果 */
.ctrl-btn-main {
  width: 144rpx;
  height: 144rpx;
  background: linear-gradient(
    155deg,
    #f8b4b4 0%,
    #ee9393 35%,
    #e57d7d 70%,
    #dc7070 100%
  );
  box-shadow: 0 12rpx 40rpx rgba(230, 130, 130, 0.45),
    0 4rpx 16rpx rgba(220, 120, 120, 0.3),
    inset 0 2rpx 4rpx rgba(255, 255, 255, 0.25);
}

.ctrl-btn-main:active {
  box-shadow: 0 6rpx 24rpx rgba(220, 120, 120, 0.5),
    inset 0 1rpx 2rpx rgba(255, 255, 255, 0.2);
}

/* 🚀 缓冲状态下的主按钮样式 */
.ctrl-btn-main.buffering {
  opacity: 0.9;
}

/* 🚀 缓冲加载动画容器 */
.buffering-spinner {
  position: relative;
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 🚀 旋转环动画 */
.spinner-ring {
  width: 48rpx;
  height: 48rpx;
  border: 4rpx solid rgba(255, 255, 255, 0.3);
  border-top-color: #ffffff;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}

/* 主按钮光晕效果 */
.main-btn-glow {
  position: absolute;
  top: -16rpx;
  left: -16rpx;
  right: -16rpx;
  bottom: -16rpx;
  border-radius: 50%;
  background: radial-gradient(
    circle,
    rgba(244, 180, 180, 0.35) 0%,
    rgba(238, 150, 150, 0.15) 50%,
    transparent 70%
  );
  pointer-events: none;
  animation: glow-pulse 2.5s ease-in-out infinite;
}

@keyframes glow-pulse {
  0%,
  100% {
    opacity: 0.6;
    transform: scale(1);
  }
  50% {
    opacity: 1;
    transform: scale(1.08);
  }
}

.ctrl-icon-main {
  width: 52rpx;
  height: 52rpx;
  position: relative;
  z-index: 1;
}

/* 播放图标（三角形）视觉重心偏左，添加左偏移使其视觉居中 */
.ctrl-icon-main.play-icon {
  margin-left: 6rpx;
}

/* -------- 功能控制区 - 统一 Toolbar -------- */
.func-toolbar {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20rpx;
  width: 100%;
  padding: 0 40rpx;
  position: relative;
  z-index: 1;
}

.func-item {
  flex: 1;
  max-width: 180rpx;
  height: 72rpx;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
  background: rgba(255, 255, 255, 0.4);
  border-radius: 36rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.5);
  backdrop-filter: blur(12rpx);
  transition: all 0.22s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  box-shadow: 0 4rpx 16rpx rgba(168, 116, 94, 0.06),
    inset 0 1rpx 2rpx rgba(255, 255, 255, 0.7);
}

.func-item:active {
  transform: scale(0.95);
  background: rgba(255, 255, 255, 0.55);
}

/* 功能按钮激活状态 */
.func-item.active {
  background: rgba(255, 255, 255, 0.92);
  border: 1rpx solid rgba(247, 196, 196, 0.7);
  box-shadow: 0 4rpx 20rpx rgba(220, 150, 150, 0.18),
    inset 0 1rpx 3rpx rgba(255, 255, 255, 0.9);
}

.func-item.active .func-icon {
  opacity: 1;
}

.func-item.active .func-label {
  color: #7a4a3a;
  opacity: 1;
}

.func-icon {
  width: 30rpx;
  height: 30rpx;
  opacity: 0.55;
  transition: opacity 0.22s ease;
}

.func-label {
  font-size: 26rpx;
  font-weight: 500;
  color: #8b5a3c;
  opacity: 0.75;
  letter-spacing: 0.5rpx;
  transition: all 0.22s ease;
}

/* -------- 底部安全区域 -------- */
.safe-bottom {
  width: 100%;
  height: env(safe-area-inset-bottom);
  min-height: 40rpx;
  flex-shrink: 0;
}

/* ========== 定时关闭弹窗 - 粉色治愈系风格 ========== */
.timer-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.timer-modal.visible {
  pointer-events: auto;
  opacity: 1;
}

/* 遮罩层 - 柔和粉色 */
.timer-mask {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(180, 140, 130, 0.25);
  backdrop-filter: blur(8rpx);
}

/* 底部面板 - 浅粉毛玻璃 */
.timer-panel {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    180deg,
    rgba(255, 248, 245, 0.96) 0%,
    rgba(255, 244, 242, 0.98) 100%
  );
  border-radius: 48rpx 48rpx 0 0;
  padding: 24rpx 40rpx 0;
  transform: translateY(100%);
  transition: transform 0.4s cubic-bezier(0.32, 0.72, 0, 1);
  backdrop-filter: blur(40rpx);
  box-shadow: 0 -8rpx 40rpx rgba(180, 140, 130, 0.15),
    0 -2rpx 10rpx rgba(180, 140, 130, 0.08);
}

.timer-modal.visible .timer-panel {
  transform: translateY(0);
}

/* 拖拽指示条 */
.panel-handle {
  width: 64rpx;
  height: 8rpx;
  background: rgba(180, 140, 130, 0.25);
  border-radius: 4rpx;
  margin: 0 auto 28rpx;
}

/* 标题区 */
.panel-header {
  margin-bottom: 32rpx;
}

.panel-title {
  font-size: 38rpx;
  font-weight: 700;
  color: #7a5542;
  letter-spacing: 1rpx;
  margin-bottom: 8rpx;
}

.panel-subtitle {
  font-size: 26rpx;
  font-weight: 400;
  color: #a67c65;
  letter-spacing: 0.5rpx;
}

/* 定时选项区 - 柔光胶囊条 */
.timer-capsule {
  background: rgba(255, 255, 255, 0.85);
  border-radius: 48rpx;
  padding: 16rpx 8rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(180, 140, 130, 0.1),
    inset 0 2rpx 4rpx rgba(255, 255, 255, 0.8);
}

.timer-scroll {
  width: 100%;
  white-space: nowrap;
}

.timer-options {
  display: inline-flex;
  align-items: center;
  padding: 0 8rpx;
  gap: 4rpx;
}

.timer-opt {
  flex-shrink: 0;
  min-width: 72rpx;
  height: 64rpx;
  padding: 0 16rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 32rpx;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.timer-opt.active {
  background: linear-gradient(145deg, #f8a5a5 0%, #e88888 50%, #e07575 100%);
  box-shadow: 0 4rpx 16rpx rgba(232, 136, 136, 0.4),
    0 2rpx 6rpx rgba(232, 136, 136, 0.2);
  transform: scale(1.05);
}

.opt-text {
  font-size: 28rpx;
  font-weight: 500;
  color: #a67c65;
  letter-spacing: 0.3rpx;
  transition: all 0.3s ease;
}

.timer-opt.active .opt-text {
  color: #ffffff;
  font-weight: 600;
  font-size: 30rpx;
  text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.1);
}

/* 单位提示 */
.timer-unit-hint {
  text-align: center;
  margin-bottom: 32rpx;
}

.unit-text {
  font-size: 24rpx;
  color: #c4a090;
  letter-spacing: 0.5rpx;
}

/* 底部快捷按钮 */
.timer-actions {
  display: flex;
  gap: 24rpx;
  margin-bottom: 24rpx;
}

.action-card {
  flex: 1;
  background: rgba(255, 255, 255, 0.9);
  border-radius: 32rpx;
  padding: 28rpx 24rpx;
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 20rpx;
  box-shadow: 0 6rpx 24rpx rgba(180, 140, 130, 0.1),
    0 2rpx 8rpx rgba(180, 140, 130, 0.06);
  transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

.action-card:active {
  transform: scale(0.97);
  box-shadow: 0 2rpx 12rpx rgba(180, 140, 130, 0.15);
  background: rgba(255, 250, 248, 0.95);
}

.action-icon-wrap {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: linear-gradient(145deg, #fff5f0 0%, #ffe8e0 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  box-shadow: 0 4rpx 12rpx rgba(180, 140, 130, 0.12);
}

.action-icon {
  width: 36rpx;
  height: 36rpx;
}

.action-content {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  flex: 1;
  min-width: 0;
}

.action-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #7a5542;
  letter-spacing: 0.3rpx;
}

.action-desc {
  font-size: 24rpx;
  font-weight: 400;
  color: #c4a090;
  letter-spacing: 0.3rpx;
}

/* 面板底部安全区 */
.panel-safe-bottom {
  height: env(safe-area-inset-bottom);
  min-height: 40rpx;
}
